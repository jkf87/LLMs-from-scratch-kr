# 4장: 텍스트를 생성하기 위해 GPT 모델을 처음부터 구현

- [flops-analysis.ipynb](flops-analysis.ipynb)는 메인 장에서 구현된 GPT 모델의 초당 부동 소수점 연산(FLOPS, floating point operations per second)을 분석합니다.
- `requirements-extra.txt`는 (`pip install -r requirements-extra.txt`를 통해) 설치해야 할 추가 Python 라이브러리를 포함합니다.